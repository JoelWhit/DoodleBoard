@model DoodleBoard.Website.Models.JoinWhiteboardViewModel

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="panel panel-default">
        <div class="panel-body">
            <form class="form-horizontal">
                <fieldset>
                    <legend>Join a Board</legend>
                    <div class="form-group">
                        <label for="inputBoard" class="col-sm-12">Board</label>
                        <div class="col-sm-12">
                            <input type="text" class="form-control" id="inputBoard">
                        </div>
                    </div>
                    <div class="form-group">
                        <div class="col-sm-9">
                            <div class="checkbox">
                                <label data-toggle="collapse" data-target="#collapseOne">
                                    <input type="checkbox" data-toggle="collapse" data-target="#pwJnCollapse" /> Password?
                                </label>
                            </div>
                        </div>
                        <div id="pwJnCollapse" class="collapse col-sm-12">
                            <input type="password" class="form-control" id="inputPassword" placeholder="Optional">
                        </div>
                    </div>
                    <legend></legend>
                    <div class="form-group">
                        <div class="col-sm-10 col-sm-offset-2">
                            <button type="submit" class="btn btn-primary">Start Drawing</button>
                        </div>
                    </div>
                </fieldset>
            </form>
        </div>
    </div>
}

